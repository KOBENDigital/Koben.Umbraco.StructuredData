# Progress log
<!-- NNN · item · date · one-line result -->
001 · scaffold + editor v1 · 15 Sep 2026 · Package repo: RCL serving a Lit bundle from App_Plugins/Koben.StructuredData; C# DataEditor on `Koben.StructuredData` (ValueType Json) with a publish-time validator; PropertyValueConverter + IDeliveryApiPropertyValueConverter resolving `$ref` media/document placeholders, stripping blanks and empty entities, `@context` first; catalogue of 18 top-level and 17 nested schema.org types with required/recommended flags; editor with sortable cards, completeness tags, enable toggle, duplicate/remove, type picker modal, allowed-types config UI, raw JSON-LD entry, and a live preview. Verified on the SQLite TestSite via the Management API (data type 201, publish of an entry without @type refused 400, Delivery API output with ImageObject width/height from a real PNG) and visually in the login-free harness. Not verified: the editor inside a real backoffice session (no credentials entered) — pickers, sorter drag, and the type-picker modal want a human pass. Known gap: nested-only definitions are not offered at top level by design; `@id` cross-references are only reachable through custom JSON-LD.
002 · generation rules · 15 Sep 2026 · Auto-generation moved out of page code into the package: a Settings → Advanced → "Structured data" workspace manages generation rules (site-wide or per document type; catalogue type + field bindings; test panel against a real page; export/import). Server: NPoco table `kobenStructuredDataRule` via AsyncMigrationBase + Upgrader on UmbracoApplicationStarting; evaluator with property/value/url/name/dates/breadcrumb/children/ref/id/entity/list sources (dot paths into picked content, fallbacks, site-root scope, name/url formats); graph service layering rules under authored entries with same-@type override; `IApiContentResponseBuilder` replaced to add `structuredDataGraph` to every Delivery API response; optional seed file for fresh environments. Verified on the TestSite via the Management API (CRUD, 400 on invalid, preview, graph, export/import) and Delivery API. Gotchas: `IPublishedStatusFilteringService` is not in DI (use `IDocumentNavigationQueryService` keys + the non-preview cache); Umbraco 18 has no sync `MigrationBase`, use `AsyncMigrationBase`; the API's scope enum must be a string or STJ emits PascalCase. Not verified: the workspace UI in a real backoffice session.
003 · rules on the Koben site · 15 Sep 2026 · Seeded the 8 Koben rules through `Koben:StructuredData:SeedFile` on the local koben-2026 DB (migration + seed logged on first boot). Two evaluator fixes came out of it: a text box converter answers "" (not null) for an untouched field, so blank strings and empty arrays now fall through to the fallback aliases; and breadcrumb items are de-duplicated by URL so a site-root container yields to the home page beneath it. Property reads also fall back to the stored source value when a converter yields null, with a Debug trace per alias.
004 · multi-site / multi-language rules · 15 Sep 2026 · Rules gain `sites` (root {key,name}, key match with name fallback) and `cultures` filters; graph service filters by the page's root and the request culture. Evaluator and resolver read `IVariationContextAccessor` for culture-aware names, property values and URLs (invariant content falls back to its single URL); `value` bindings accept `cultureValues`; new `dictionary` source via `IDictionaryItemService` behind a 5-minute cache (sync-over-async, one query per key). Management API: `GET sites` (roots + hostnames via IDomainService), `GET cultures`, preview accepts `culture`. Migration `rules-table-v2` adds the two columns with raw `ALTER TABLE … ADD` per provider because Umbraco's Alter builder throws on SQLite. Editor: Sites and Languages checkbox groups (hidden with one of each), language selector on the test panel, per-language inputs for fixed values, dictionary source. Verified on the TestSite with a second language and a second root: culture-filtered rule absent on the default request and present with `Accept-Language: fr-FR`, absent on the other root, dictionary and per-culture value resolve per language in preview.
